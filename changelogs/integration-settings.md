# Integration Settings

- added a FastAPI settings API with server claim, connection management, OAuth callback, and internal credential endpoints
- added encrypted credential handling plus a typed provider registry and bot-side credential accessor
- added a guild-scoped Discord `/settings` command that links users into the settings web flow
- added the initial React/Vite settings frontend scaffold and the Supabase migration for `servers` and `connections`
