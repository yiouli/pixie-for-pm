[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "pixie-for-pm"
version = "0.1.0"
description = "Multi-agent product collaboration scaffold for Discord and LangGraph."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.11.18",
    "discord.py>=2.5.2",
    "langgraph>=0.4.7",
    "langgraph-checkpoint-sqlite>=2.0.6",
]

[project.scripts]
pixie-discord-bot = "pixie_for_pm.main:main"

[dependency-groups]
dev = [
    "mypy>=1.15.0",
    "pytest>=8.3.5",
    "pytest-asyncio>=0.26.0",
    "ruff>=0.11.7",
]

[tool.hatch.build.targets.wheel]
packages = ["pixie_for_pm"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.11"
packages = ["pixie_for_pm", "tests"]
strict = true
warn_unreachable = true
